network:
  version: 2
  ethernets:
    {{ interface }}:
      addresses: [{{ ip }}/24]
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses: {{ dns_servers }}
